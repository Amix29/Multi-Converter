# Compressed bundled engines

Windows packaging stages the already verified LibreOffice engine ZIP here.
The archive is extracted locally on first LibreOffice use and never downloaded
at runtime. Generated archives are ignored by Git.
