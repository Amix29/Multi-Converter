import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";

const root = process.cwd();
const packageJson = JSON.parse(fs.readFileSync(path.join(root, "package.json"), "utf8"));
const tauriConfig = JSON.parse(fs.readFileSync(path.join(root, "src-tauri", "tauri.conf.json"), "utf8"));
const linuxConfig = JSON.parse(fs.readFileSync(path.join(root, "src-tauri", "tauri.linux.conf.json"), "utf8"));
const linuxEngineConfig = JSON.parse(fs.readFileSync(path.join(root, "tools", "engine-packages.linux.config.json"), "utf8"));
const prepareScript = fs.readFileSync(path.join(root, "scripts", "prepare-bundled-engines.mjs"), "utf8");
const validateScript = fs.readFileSync(path.join(root, "scripts", "validate-bundled-engines.mjs"), "utf8");
const linuxEngineSourcesPreparer = fs.readFileSync(path.join(root, "scripts", "prepare-linux-engine-sources.mjs"), "utf8");
const linuxEngineReleasePreparer = fs.readFileSync(path.join(root, "scripts", "prepare-linux-engine-release-assets.mjs"), "utf8");
const linuxEnvironmentTest = fs.readFileSync(path.join(root, "scripts", "test-linux-environment.mjs"), "utf8");
const linuxHostTest = fs.readFileSync(path.join(root, "scripts", "test-linux-host.mjs"), "utf8");
const linuxConversionTest = fs.readFileSync(path.join(root, "scripts", "test-linux-conversions.mjs"), "utf8");
const linuxSidecarPreparer = fs.readFileSync(path.join(root, "scripts", "prepare-linux-sidecars.mjs"), "utf8");
const linuxSidecarReleasePreparer = fs.readFileSync(path.join(root, "scripts", "prepare-linux-sidecar-release-assets.mjs"), "utf8");
const linuxAppImageVerifier = fs.readFileSync(path.join(root, "scripts", "verify-linux-appimage.mjs"), "utf8");
const linuxPdfiumPrepare = fs.readFileSync(path.join(root, "scripts", "prepare-pdfium-engine-linux.mjs"), "utf8");
const linuxLibreOfficePrepare = fs.readFileSync(path.join(root, "scripts", "prepare-libreoffice-engine-linux.mjs"), "utf8");
const linuxPandocPrepare = fs.readFileSync(path.join(root, "scripts", "prepare-pandoc-engine-linux.mjs"), "utf8");
const linuxLibvipsPrepare = fs.readFileSync(path.join(root, "scripts", "prepare-libvips-engine-linux.mjs"), "utf8");
const elfLibrary = fs.readFileSync(path.join(root, "scripts", "lib", "elf.mjs"), "utf8");
const ffmpegVersionLibrary = fs.readFileSync(path.join(root, "scripts", "lib", "ffmpeg-version.mjs"), "utf8");
const linuxCiGate = fs.readFileSync(path.join(root, "scripts", "test-linux-ci-gate.mjs"), "utf8");
const releaseAssetValidator = fs.readFileSync(path.join(root, "scripts", "validate-release-assets.mjs"), "utf8");
const releaseAssetPreparer = fs.readFileSync(path.join(root, "scripts", "prepare-release-assets.mjs"), "utf8");
const readme = fs.readFileSync(path.join(root, "README.md"), "utf8");

assert.equal(packageJson.scripts["test:linux-packaging"], "node scripts/test-linux-packaging.mjs", "Linux packaging contract test must be exposed through npm");
assert.equal(packageJson.scripts["test:linux:environment"], "node scripts/test-linux-environment.mjs", "Linux environment validation must be exposed through npm");
assert.equal(packageJson.scripts["test:linux:ci"], "node scripts/test-linux-ci-gate.mjs", "Linux CI gate must be exposed through npm");
assert.equal(packageJson.scripts["test:linux:host"], "node scripts/test-linux-host.mjs", "Linux host validation must be exposed through npm");
assert.equal(packageJson.scripts["test:linux:conversions"], "node scripts/test-linux-conversions.mjs", "Linux conversion validation must be exposed through npm");
assert.equal(packageJson.scripts["test:linux-sidecars"], "node scripts/test-linux-sidecars.mjs", "Linux sidecar staging tests must be exposed through npm");
assert.equal(packageJson.scripts["test:linux-sidecar-release-assets"], "node scripts/test-linux-sidecar-release-assets.mjs", "Linux sidecar release asset tests must be exposed through npm");
assert.equal(packageJson.scripts["test:linux-engine-release-assets"], "node scripts/test-linux-engine-release-assets.mjs", "Linux staged engine asset tests must be exposed through npm");
assert.equal(packageJson.scripts["test:elf"], "node scripts/test-elf.mjs", "Shared ELF parser tests must be exposed through npm");
assert.match(packageJson.scripts.check, /test:elf/, "The default check gate must cover shared ELF parser tests");
assert.match(packageJson.scripts.check, /test:linux-engine-release-assets/, "The default check gate must cover Linux staged engine assets");
assert.equal(packageJson.scripts["prepare:linux-sidecars"], "node scripts/prepare-linux-sidecars.mjs", "Linux sidecar staging must be exposed through npm");
assert.equal(packageJson.scripts["prepare:linux-sidecar-release-assets"], "node scripts/prepare-linux-sidecar-release-assets.mjs", "Linux sidecar release asset staging must be exposed through npm");
assert.equal(packageJson.scripts["package:linux-engines"], "node scripts/package-engines.mjs --config tools/engine-packages.linux.config.json --output dist-engines-linux", "Linux engine packaging must be exposed through npm");
assert.equal(packageJson.scripts["prepare:linux-engine-sources"], "node scripts/prepare-linux-engine-sources.mjs", "Linux engine source preparation must be exposed through npm");
assert.equal(packageJson.scripts["prepare:linux-engine-release-assets"], "node scripts/prepare-linux-engine-release-assets.mjs", "Linux staged engine release helper must be exposed through npm");
assert.equal(packageJson.scripts["prepare:pdfium-engine:linux"], "node scripts/prepare-pdfium-engine-linux.mjs", "Linux PDFium upstream preparation must be exposed through npm");
assert.equal(packageJson.scripts["prepare:libreoffice-engine:linux"], "node scripts/prepare-libreoffice-engine-linux.mjs", "Linux LibreOffice upstream preparation must be exposed through npm");
assert.equal(packageJson.scripts["prepare:pandoc-engine:linux"], "node scripts/prepare-pandoc-engine-linux.mjs", "Linux Pandoc upstream preparation must be exposed through npm");
assert.equal(packageJson.scripts["prepare:libvips-engine:linux"], "node scripts/prepare-libvips-engine-linux.mjs", "Linux libvips runtime preparation must be exposed through npm");
assert.match(packageJson.scripts["prepare:linux-upstream-engines"], /prepare:pdfium-engine:linux/, "Linux upstream preparation must include PDFium");
assert.match(packageJson.scripts["prepare:linux-upstream-engines"], /prepare:libreoffice-engine:linux/, "Linux upstream preparation must include LibreOffice");
assert.match(packageJson.scripts["prepare:linux-upstream-engines"], /prepare:pandoc-engine:linux/, "Linux upstream preparation must include Pandoc");
assert.match(packageJson.scripts["prepare:linux-upstream-engines"], /prepare:libvips-engine:linux/, "Linux upstream preparation must include libvips");
assert.equal(packageJson.scripts["verify:linux-appimage"], "node scripts/verify-linux-appimage.mjs", "Linux AppImage verification must be exposed through npm");
assert.match(packageJson.scripts["tauri:build:linux"], /x86_64-unknown-linux-gnu/, "Linux build must target Linux x64 explicitly");
assert.match(packageJson.scripts["tauri:build:linux"], /--bundles appimage/, "Linux build must produce the universal AppImage installer");
assert.match(packageJson.scripts["tauri:build:linux"], /tauri\.linux\.conf\.json/, "Linux build must use the Linux-specific Tauri config");

assert.deepEqual(linuxConfig.bundle.targets, ["appimage"], "Linux release must build one AppImage installer");
assert.equal(linuxConfig.bundle.createUpdaterArtifacts, true, "Linux releases must create updater signatures");
assert.deepEqual(linuxConfig.bundle.externalBin, tauriConfig.bundle.externalBin, "Linux config must keep the same sidecar stems");
assert.deepEqual(linuxConfig.bundle.resources, tauriConfig.bundle.resources, "Linux config must keep bundled engine resources");
assert.equal(linuxConfig.bundle.linux.appimage.bundleMediaFramework, false, "Linux AppImage should not bundle unused media playback frameworks");

assert.match(prepareScript, /linux-x64/, "Bundled engine preparation must recognize Linux x64");
assert.match(prepareScript, /ffmpeg-x86_64-unknown-linux-gnu/, "Bundled engine preparation must stage the Linux FFmpeg sidecar");
assert.match(prepareScript, /engine-sources", "linux-x64"/, "Linux sidecars must come from explicit Linux engine sources");
assert.match(prepareScript, /MULTI_CONVERTER_REQUIRE_ADVANCED_ENGINES/, "Bundled engine preparation must support strict advanced-engine release mode");
assert.match(prepareScript, /extractZipArchive/, "Bundled engine preparation must extract ZIP-based advanced engine archives on Linux");
assert.match(prepareScript, /unzip", \["-q"/, "Bundled engine preparation must use unzip for ZIP archives on non-Windows hosts");
assert.ok((prepareScript.match(/extractArchive\(archive, extractDir, engine\.archiveType\)/g) ?? []).length >= 2, "Bundled engine preparation must preserve manifest archive types for sidecars and advanced engines");
assert.match(prepareScript, /patchelf/, "Bundled engine preparation must patch Linux engine RPATHs for AppImage packaging");
assert.match(prepareScript, /--set-rpath/, "Bundled engine preparation must expose portable Linux engine libraries through ELF RPATHs");
assert.match(prepareScript, /libkf5be1lo\.so/, "Bundled engine preparation must prune optional LibreOffice KDE backends from Linux headless bundles");
assert.match(prepareScript, /lo_kde5filepicker/, "Bundled engine preparation must prune optional LibreOffice KDE file picker from Linux headless bundles");
assert.match(prepareScript, /libavmediagtk\.so/, "Bundled engine preparation must prune optional LibreOffice GTK media backend from Linux headless bundles");
assert.match(prepareScript, /libavmediaqt6\.so/, "Bundled engine preparation must prune optional LibreOffice Qt media backend from Linux headless bundles");
assert.match(prepareScript, /libvclplug_gtk3_kde5lo\.so/, "Bundled engine preparation must prune optional LibreOffice GTK/KDE VCL backend from Linux headless bundles");
assert.match(prepareScript, /liblibreofficekitgtk\.so/, "Bundled engine preparation must prune optional LibreOfficeKit GTK backend from Linux headless bundles");
assert.match(prepareScript, /libofficebean\.so/, "Bundled engine preparation must prune optional LibreOffice Java AWT bean from Linux headless bundles");
assert.match(prepareScript, /_crypt\\.cpython-\[\^\/\]\+\\.so/, "Bundled engine preparation must prune optional LibreOffice Python crypt module from Linux headless bundles");
assert.match(validateScript, /linux-x64/, "Bundled engine validation must recognize Linux x64");
assert.match(validateScript, /x86_64-unknown-linux-gnu/, "Bundled engine validation must require Linux x64 sidecars");
assert.match(validateScript, /fichier Linux non-ELF/, "Bundled engine validation must reject non-ELF Linux sidecars even when smoke tests are skipped");
assert.match(validateScript, /chemin binaire non-Linux dans le manifeste/, "Bundled engine validation must reject foreign binary paths in Linux advanced engine manifests");
assert.match(validateScript, /platform !== "macos-universal" && platform !== "linux-x64"/, "Bundled engine validation must score Linux engine binaries by native architecture");
assert.match(validateScript, /strict release validation requires platform-specific advanced engines/, "Bundled engine validation must fail in strict mode when advanced engines are missing");
assert.match(validateScript, /LD_LIBRARY_PATH/, "Bundled engine validation must smoke-test Linux portable engine libraries");
assert.match(validateScript, /VIPS_MODULE_PATH/, "Bundled engine validation must smoke-test Linux libvips modules");
assert.equal(linuxEngineConfig.platform, "linux-x64", "Linux engine package config must target linux-x64");
assert.deepEqual(
  linuxEngineConfig.engines.map((engine) => engine.engineId),
  ["pdfium", "libreoffice", "pandoc", "libvips"],
  "Linux engine package config must cover the advanced engine release assets",
);
assert.doesNotMatch(JSON.stringify(linuxEngineConfig), /ffmpeg|ffprobe/i, "Linux engine package config must not duplicate FFmpeg sidecars; they are staged through prepare:linux-sidecars");
for (const engine of linuxEngineConfig.engines) {
  assert.equal(engine.platform, "linux-x64", `${engine.engineId} must be declared for linux-x64`);
  assert.match(engine.sourceDir, /^engine-sources\/linux-x64\//, `${engine.engineId} must use Linux staged engine sources`);
  for (const relative of engine.binaryPaths) {
    assert.doesNotMatch(relative, /\.(exe|dll|dylib)$/i, `${engine.engineId} Linux binary path must not point to non-Linux files`);
  }
}
assert.match(linuxEngineSourcesPreparer, /process\.platform !== "linux"/, "Linux engine source preparation must refuse non-Linux hosts");
assert.match(linuxEngineSourcesPreparer, /pdfium[\s\S]*libreoffice[\s\S]*pandoc[\s\S]*libvips/, "Linux engine source preparation must cover every advanced engine");
assert.match(linuxEngineSourcesPreparer, /allow-existing/, "Linux engine source preparation must explicitly support already prepared source trees");
assert.match(linuxEngineSourcesPreparer, /using existing validated Linux source tree/, "Linux engine source preparation must validate existing source trees before reuse");
assert.match(linuxEngineSourcesPreparer, /verifySha256/, "Linux engine source preparation must verify source archive checksums");
assert.match(linuxEngineSourcesPreparer, /tar\.xz|tar\\\.xz/, "Linux engine source preparation must accept common .tar.xz source archives");
assert.match(linuxEngineSourcesPreparer, /-xJf/, "Linux engine source preparation must explicitly extract .tar.xz archives with xz support");
assert.match(linuxEngineSourcesPreparer, /Refusing non-HTTPS Linux engine archive URL/, "Linux engine source preparation must reject non-HTTPS URLs");
assert.match(linuxEngineSourcesPreparer, /non-Linux file found in source tree/, "Linux engine source preparation must reject Windows/macOS binaries");
assert.match(linuxEngineSourcesPreparer, /\\\.\(app\|bat\|cmd\|dll\|dmg\|dylib\|exe\|msi\|pkg\|ps1\)/, "Linux engine source preparation must reject non-Linux binary path segments, not only final filenames");
assert.match(linuxEngineSourcesPreparer, /path\.relative\(sourceRoot, filePath\)/, "Linux engine source preparation must inspect extracted source paths relative to the archive root");
assert.match(linuxEngineSourcesPreparer, /licenses\/LICENSE\.txt/, "Linux engine source preparation must require license files");
assert.match(linuxEngineSourcesPreparer, /licenses\/THIRD_PARTY_NOTICES\.txt/, "Linux engine source preparation must require third-party notices");
assert.doesNotMatch(linuxEngineSourcesPreparer, /new URLOrPath/, "Linux engine source preparation must not call URL/path helpers as constructors");
assert.match(linuxEngineReleasePreparer, /platform = "linux-x64"/, "Linux staged engine helper must filter linux-x64 manifest entries");
assert.match(linuxEngineReleasePreparer, /No advanced \$\{platform\} engine entries found/, "Linux staged engine helper must fail without advanced Linux engine entries");
assert.match(linuxEngineReleasePreparer, /requiredAdvancedEngines = \["pdfium", "libreoffice", "pandoc", "libvips"\]/, "Linux staged engine helper must require every advanced Linux engine");
assert.match(linuxEngineReleasePreparer, /Missing required advanced \$\{platform\} engine entries/, "Linux staged engine helper must fail on partial advanced Linux engine manifests");
assert.match(linuxEngineReleasePreparer, /Duplicate advanced \$\{platform\} engine entries/, "Linux staged engine helper must fail on duplicate advanced Linux engine entries");
assert.match(linuxEngineReleasePreparer, /Unexpected advanced \$\{platform\} engine entries/, "Linux staged engine helper must fail on unexpected advanced Linux engine entries");
assert.match(linuxEngineReleasePreparer, /Unexpected non-advanced \$\{platform\} engine entries/, "Linux staged engine helper must fail on non-advanced Linux engine entries");
assert.match(linuxEngineReleasePreparer, /verifySha256/, "Linux staged engine helper must verify engine archive checksums");
assert.match(linuxEngineReleasePreparer, /binaryPaths must not reference non-Linux files/, "Linux staged engine helper must reject non-Linux engine paths");
assert.match(linuxEngineReleasePreparer, /Unexpected local Linux engine assets/, "Linux staged engine helper must reject extra files in local workflow artifacts");
assert.match(linuxPdfiumPrepare, /process\.platform !== "linux" \|\| process\.arch !== "x64"/, "Linux PDFium preparation must refuse non-Linux x64 hosts");
assert.match(linuxPdfiumPrepare, /pdfium-linux-x64\.tgz/, "Linux PDFium preparation must use the upstream Linux x64 PDFium archive");
assert.match(linuxPdfiumPrepare, /PDFIUM_LINUX_X64_ARCHIVE_SHA256/, "Linux PDFium preparation must require a pinned upstream SHA-256");
assert.match(linuxPdfiumPrepare, /pdfium-render-x86_64-unknown-linux-gnu/, "Linux PDFium preparation must stage the wrapper name used by the Linux engine config");
assert.match(linuxPdfiumPrepare, /PDFIUM_LIBRARY_PATH/, "Linux PDFium preparation must smoke-test the wrapper against the staged library");
assert.match(linuxLibreOfficePrepare, /process\.platform !== "linux" \|\| process\.arch !== "x64"/, "Linux LibreOffice preparation must refuse non-Linux x64 hosts");
assert.match(linuxLibreOfficePrepare, /Linux_x86-64_deb\.tar\.gz/, "Linux LibreOffice preparation must use the official Linux x86-64 deb archive");
assert.match(linuxLibreOfficePrepare, /LIBREOFFICE_LINUX_X64_DEB_ARCHIVE_SHA256/, "Linux LibreOffice preparation must require a pinned upstream SHA-256");
assert.match(linuxLibreOfficePrepare, /dpkg-deb/, "Linux LibreOffice preparation must extract official Debian packages without installing them into the runner");
assert.match(linuxLibreOfficePrepare, /removeNonLinuxPayloadFiles/, "Linux LibreOffice preparation must prune non-Linux payload files before strict source validation");
assert.match(linuxLibreOfficePrepare, /--terminate_after_init/, "Linux LibreOffice preparation must smoke-test headless startup");
assert.match(linuxPandocPrepare, /process\.platform !== "linux" \|\| process\.arch !== "x64"/, "Linux Pandoc preparation must refuse non-Linux x64 hosts");
assert.match(linuxPandocPrepare, /linux-amd64\.tar\.gz/, "Linux Pandoc preparation must use the official Linux amd64 archive");
assert.match(linuxPandocPrepare, /PANDOC_LINUX_X64_ARCHIVE_SHA256/, "Linux Pandoc preparation must require a pinned upstream SHA-256");
assert.match(linuxPandocPrepare, /bin", "pandoc"/, "Linux Pandoc preparation must stage the binary path used by the Linux engine config");
assert.match(linuxPandocPrepare, /Bonjour Multi-Converter/, "Linux Pandoc preparation must run a real Markdown-to-HTML smoke test");
assert.match(linuxLibvipsPrepare, /process\.platform !== "linux" \|\| process\.arch !== "x64"/, "Linux libvips preparation must refuse non-Linux x64 hosts");
assert.match(linuxLibvipsPrepare, /libvips-tools/, "Linux libvips preparation must install or require Ubuntu libvips tools");
assert.match(linuxLibvipsPrepare, /ldd/, "Linux libvips preparation must inspect dynamic dependencies");
assert.match(linuxLibvipsPrepare, /LD_LIBRARY_PATH/, "Linux libvips preparation must smoke-test against packaged libraries");
assert.match(linuxLibvipsPrepare, /VIPS_MODULE_PATH/, "Linux libvips preparation must stage and smoke-test libvips modules");

assert.match(linuxEnvironmentTest, /pkg-config/, "Linux environment validation must check native pkg-config dependencies");
assert.match(linuxEnvironmentTest, /dbus-1/, "Linux environment validation must check DBus development headers");
assert.match(linuxEnvironmentTest, /current checkout is on a Windows-mounted filesystem inside WSL/, "Linux environment validation must reject Windows-mounted checkouts inside WSL");
assert.match(linuxEnvironmentTest, /Windows-mounted executable inside WSL/, "Linux environment validation must reject Windows-mounted tools inside WSL");
assert.match(linuxEnvironmentTest, /@tauri-apps\/cli/, "Linux environment validation must catch missing Tauri Linux native bindings");
assert.match(linuxHostTest, /process\.platform !== "linux"/, "Linux host validation must refuse non-Linux hosts");
assert.match(linuxHostTest, /CI placeholder sidecar/, "Linux host validation must reject compile-only placeholder sidecars");
assert.match(linuxHostTest, /sidecar is not an x86_64 ELF executable/, "Linux host validation must reject non-x86_64 Linux sidecars before smoke tests");
assert.match(linuxHostTest, /from "\.\/lib\/elf\.mjs"/, "Linux host validation must use the shared ELF parser");
assert.match(linuxHostTest, /from "\.\/lib\/ffmpeg-version\.mjs"/, "Linux host validation must read the canonical FFmpeg version from Rust");
assert.match(linuxHostTest, /MULTI_CONVERTER_ENGINE_PLATFORM:\s*"linux-x64"/, "Linux host validation must validate Linux bundled engines");
assert.match(linuxHostTest, /MULTI_CONVERTER_REQUIRE_ADVANCED_ENGINES:\s*"1"/, "Linux host validation must require Linux advanced engines for release validation");
assert.match(linuxConversionTest, /process\.platform !== "linux"/, "Linux conversion validation must refuse non-Linux hosts");
assert.match(linuxConversionTest, /requiredAdvancedEngines = \["pdfium", "libreoffice", "pandoc", "libvips"\]/, "Linux conversion validation must require the advanced engine set");
assert.match(linuxConversionTest, /Missing \$\{platform\} advanced engine manifest entry/, "Linux conversion validation must fail clearly when Linux advanced engines are missing");
assert.match(linuxConversionTest, /normalizeManifestPath/, "Linux conversion validation must normalize advanced engine binary paths before trusting them");
assert.match(linuxConversionTest, /must not contain empty, current or parent path segments/, "Linux conversion validation must reject traversing advanced engine binary paths");
assert.match(linuxConversionTest, /\\\.\(app\|bat\|cmd\|dll\|dmg\|dylib\|exe\|msi\|pkg\|ps1\)/, "Linux conversion validation must reject non-Linux binary path segments, not only final filenames");
assert.match(linuxConversionTest, /MULTI_CONVERTER_REQUIRE_ADVANCED_ENGINES:\s*"1"/, "Linux conversion validation must run bundled engine preparation and validation in strict advanced-engine mode");
assert.match(linuxConversionTest, /test:linux:host/, "Linux conversion validation must reuse the strict real-sidecar host gate");
assert.match(linuxConversionTest, /test:conversions/, "Linux conversion validation must run the full conversion matrix");
assert.match(linuxSidecarPreparer, /ffmpeg-x86_64-unknown-linux-gnu/, "Linux sidecar staging must require the Linux FFmpeg sidecar name");
assert.match(linuxSidecarPreparer, /ffprobe-x86_64-unknown-linux-gnu/, "Linux sidecar staging must require the Linux ffprobe sidecar name");
assert.match(linuxSidecarPreparer, /verifySha256/, "Linux sidecar staging must verify SHA-256 files before copying sidecars");
assert.match(linuxSidecarPreparer, /placeholder sidecar rejected/, "Linux sidecar staging must reject compile-only placeholders");
assert.match(linuxSidecarPreparer, /staged Linux sidecar is not an ELF executable/, "Linux sidecar staging must reject non-ELF sidecar files before packaging");
assert.match(linuxSidecarPreparer, /staged Linux sidecar is not an x86_64 ELF executable/, "Linux sidecar staging must reject non-x86_64 Linux sidecars before packaging");
assert.match(linuxSidecarPreparer, /from "\.\/lib\/elf\.mjs"/, "Linux sidecar staging must use the shared ELF parser");
assert.match(linuxSidecarPreparer, /from "\.\/lib\/ffmpeg-version\.mjs"/, "Linux sidecar staging must default to the canonical FFmpeg version from Rust");
assert.match(linuxSidecarPreparer, /Unexpected Linux sidecar assets/, "Linux sidecar staging must reject extra files in staging artifact directories");
assert.match(linuxSidecarPreparer, /process\.platform !== "linux"/, "Linux sidecar staging smoke tests must refuse non-Linux hosts");
assert.match(linuxSidecarReleasePreparer, /FFMPEG_LINUX_X64_BINARY/, "Linux sidecar release assets must come from an explicit Linux FFmpeg source");
assert.match(linuxSidecarReleasePreparer, /FFPROBE_LINUX_X64_BINARY/, "Linux sidecar release assets must come from an explicit Linux ffprobe source");
assert.match(linuxSidecarReleasePreparer, /verifySha256/, "Linux sidecar release asset staging must verify source checksums");
assert.match(linuxSidecarReleasePreparer, /Refusing non-HTTPS Linux sidecar source URL/, "Linux sidecar release asset staging must reject non-HTTPS URLs");
assert.match(linuxSidecarReleasePreparer, /extractArchive/, "Linux sidecar release asset staging must accept checksum-pinned archives from reviewed providers");
assert.match(linuxSidecarReleasePreparer, /tar\.xz|tar\\\.xz/, "Linux sidecar release asset staging must accept common .tar.xz FFmpeg archives from reviewed providers");
assert.match(linuxSidecarReleasePreparer, /-xJf/, "Linux sidecar release asset staging must explicitly extract .tar.xz archives with xz support");
assert.match(linuxSidecarReleasePreparer, /not an AppImage/, "Linux sidecar release asset staging must reject AppImages as sidecar inputs");
assert.match(linuxSidecarReleasePreparer, /source must not be inside the output directory/, "Linux sidecar release asset staging must reject local sources inside the cleaned output directory");
assert.match(linuxSidecarReleasePreparer, /staged Linux sidecar is not an x86_64 ELF executable/, "Linux sidecar release asset staging must reject non-x86_64 Linux sidecars");
assert.match(linuxSidecarReleasePreparer, /Linux sidecar smoke tests must run on Linux x64/, "Linux sidecar release asset staging must smoke-test on Linux x64");
assert.match(linuxSidecarReleasePreparer, /from "\.\/lib\/ffmpeg-version\.mjs"/, "Linux sidecar release staging must default to the canonical FFmpeg version from Rust");
assert.match(linuxAppImageVerifier, /--appimage-extract/, "Linux AppImage verification must extract the AppImage without launching the UI");
assert.match(linuxAppImageVerifier, /Linux AppImage is not an x86_64 ELF executable/, "Linux AppImage verification must reject non-x86_64 AppImages");
assert.match(linuxAppImageVerifier, /from "\.\/lib\/elf\.mjs"/, "Linux AppImage verification must use the shared ELF parser");
assert.match(linuxAppImageVerifier, /from "\.\/lib\/ffmpeg-version\.mjs"/, "Linux AppImage verification must read the canonical FFmpeg version from Rust");
assert.match(linuxAppImageVerifier, /verifyLinuxSidecar\(appDir, "ffmpeg"\)/, "Linux AppImage verification must check the bundled FFmpeg sidecar");
assert.match(linuxAppImageVerifier, /verifyLinuxSidecar\(appDir, "ffprobe"\)/, "Linux AppImage verification must check the bundled ffprobe sidecar");
assert.match(linuxAppImageVerifier, /assertLinuxElfExecutable\(selected, `\$\{stem\} sidecar`\)/, "Linux AppImage verification must reject non-x86_64 bundled sidecars after extraction");
assert.match(linuxAppImageVerifier, /verifyUpdaterSignature\(appImagePath, signaturePath\)/, "Linux AppImage verification must cryptographically verify the updater signature");
assert.match(linuxAppImageVerifier, /updater-signature-verifier/, "Linux AppImage verification must use the shared updater signature verifier");
assert.match(linuxAppImageVerifier, /requiredAdvancedEngines = \["pdfium", "libreoffice", "pandoc", "libvips"\]/, "Linux AppImage verification must require every advanced bundled engine");
assert.match(linuxAppImageVerifier, /missing required advanced bundled engines/, "Linux AppImage verification must fail when advanced engines are missing");
assert.match(linuxAppImageVerifier, /duplicate advanced bundled engines/, "Linux AppImage verification must reject duplicate advanced bundled engines");
assert.match(linuxAppImageVerifier, /unexpected advanced bundled engines/, "Linux AppImage verification must reject unexpected advanced bundled engines");
assert.match(linuxAppImageVerifier, /Unexpected non-advanced Linux bundled engine metadata/, "Linux AppImage verification must reject non-advanced bundled engine metadata");
assert.match(linuxAppImageVerifier, /binaryPaths must not reference non-Linux files/, "Linux AppImage verification must reject non-Linux binary path segments in bundled engine metadata");
assert.match(linuxAppImageVerifier, /\^\[A-Za-z\]:\\\//, "Linux AppImage verification must reject absolute Windows-style bundled engine metadata paths");
assert.match(linuxAppImageVerifier, /Non-Linux file found in Linux AppImage/, "Linux AppImage verification must reject foreign-platform bundled files");
assert.match(linuxAppImageVerifier, /Non-Linux sidecar found in Linux AppImage/, "Linux AppImage verification must reject foreign-platform FFmpeg sidecars without relying on filename extensions");
assert.match(linuxAppImageVerifier, /dylib/, "Linux AppImage verification must reject obvious macOS-only bundled files");
assert.match(linuxCiGate, /prepare-tauri-ci-sidecars\.mjs", "--target", "x86_64-unknown-linux-gnu"/, "Linux CI gate must stage compile-only sidecars");
assert.match(linuxCiGate, /test:linux-engine-release-assets/, "Linux CI gate must cover staged Linux engine release asset validation");
assert.doesNotMatch(linuxCiGate, /test:linux:host/, "Linux CI gate must not claim real sidecar validation");

assert.match(releaseAssetValidator, /linux-x86_64/, "Release asset validation must include the Linux updater platform key");
assert.match(releaseAssetValidator, /Multi-Converter_\$\{version\}_linux-x64\.AppImage/, "Release validation must require the versioned Linux AppImage");
assert.match(releaseAssetValidator, /not an x86_64 ELF executable/, "Release asset validation must reject non-x86_64 Linux AppImages");
assert.match(releaseAssetPreparer, /--linux-appimage/, "Release asset preparation must accept a verified Linux AppImage");
assert.match(releaseAssetPreparer, /Multi-Converter_linux-x64\.AppImage/, "Release asset preparation must create the stable Linux download alias");
assert.match(releaseAssetPreparer, /assertSourceVersion\(linuxAppImageSource, "Linux AppImage", \{ required: true \}\)/, "Release asset preparation must reject stale or unversioned Linux AppImage sources");
assert.match(releaseAssetPreparer, /Linux AppImage source is not an x86_64 ELF executable/, "Release asset preparation must reject non-x86_64 Linux AppImage sources before copying");
assert.match(releaseAssetPreparer, /from "\.\/lib\/elf\.mjs"/, "Release asset preparation must use the shared ELF parser for Linux AppImages");
const linuxReleaseArtifactPreparer = fs.readFileSync(path.join(root, "scripts", "prepare-linux-release-artifacts.mjs"), "utf8");
assert.match(linuxReleaseArtifactPreparer, /filename must include version/, "Linux release artifact preparation must reject unversioned AppImage sources");
assert.match(linuxReleaseArtifactPreparer, /not an x86_64 ELF executable/, "Linux release artifact preparation must reject non-x86_64 AppImage sources");
assert.match(linuxReleaseArtifactPreparer, /from "\.\/lib\/elf\.mjs"/, "Linux release artifact preparation must use the shared ELF parser");
assert.match(elfLibrary, /ELF_MACHINE_X86_64 = 0x3e/, "Shared ELF parser must identify x86_64 ELF binaries");
assert.match(ffmpegVersionLibrary, /FFMPEG_REQUIRED_VERSION/, "Shared FFmpeg version helper must read the runtime version contract from Rust");
assert.match(readme, /Multi-Converter_linux-x64\.AppImage/, "README must link to the stable latest Linux AppImage");

console.log("Linux packaging contract tests passed.");
